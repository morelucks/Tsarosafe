# WalletConnect Project ID
# Get your project ID from https://cloud.reown.com
# Required for wallet connection to work
NEXT_PUBLIC_WALLETCONNECT_PROJECT_ID=your_walletconnect_project_id_here

# Self Verification Endpoint (Optional)
# Default: https://playground.self.xyz/api/verify
# Only set this if you want to use a custom Self verification endpoint
# NEXT_PUBLIC_SELF_ENDPOINT=https://playground.self.xyz/api/verify
